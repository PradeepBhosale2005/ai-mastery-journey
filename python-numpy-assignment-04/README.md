# NumPy Assignment 04

## Assignment Summary

This folder contains Python NumPy solutions for 10 assignment sections. Each assignment section is saved in a separate Python file.

Topics covered:

1. Array Creation and Manipulation
2. Array Indexing and Slicing
3. Array Operations
4. Statistical Operations
5. Broadcasting
6. Linear Algebra
7. Advanced Array Manipulation
8. Fancy Indexing and Boolean Indexing
9. Structured Arrays
10. Masked Arrays

## Learning Goals

- Create and manipulate NumPy arrays.
- Use indexing and slicing on multidimensional arrays.
- Perform element-wise array operations.
- Calculate statistical values such as mean, median, standard deviation, and variance.
- Apply broadcasting with 1D and 2D arrays.
- Use NumPy linear algebra functions.
- Reshape, flatten, and restore arrays.
- Use fancy indexing and boolean indexing.
- Work with structured arrays.
- Work with masked arrays.

## Requirements

- Python installed on the system
- Visual Studio Code or any code editor
- NumPy installed

Install NumPy using:

```bash
pip install numpy
```

Or install from the included requirements file:

```bash
pip install -r requirements.txt
```

## Project Structure

```text
python-numpy-assignment-04/
├── README.md
├── requirements.txt
├── assignment_01_array_creation.py
├── assignment_02_indexing_slicing.py
├── assignment_03_array_operations.py
├── assignment_04_statistical_operations.py
├── assignment_05_broadcasting.py
├── assignment_06_linear_algebra.py
├── assignment_07_advanced_array_manipulation.py
├── assignment_08_fancy_boolean_indexing.py
├── assignment_09_structured_arrays.py
├── assignment_10_masked_arrays.py
├── run_all.py
└── test_numpy_assignment.py
```

## How to Run After Extracting ZIP File

### Step 1: Extract the ZIP File

Extract the submitted ZIP file on your system.

### Step 2: Open the Folder

Open the extracted folder in Visual Studio Code or any code editor.

### Step 3: Open Terminal

Open PowerShell, Command Prompt, or the integrated terminal in Visual Studio Code.

### Step 4: Go to the Assignment Folder

```bash
cd python-numpy-assignment-04
```

### Step 5: Install NumPy

```bash
pip install -r requirements.txt
```

If `pip` does not work, try:

```bash
python -m pip install -r requirements.txt
```

### Step 6: Run All Assignment Sections

```bash
python run_all.py
```

If `python` does not work, try:

```bash
py run_all.py
```

### Step 7: Run a Single Assignment File

Example:

```bash
python assignment_01_array_creation.py
```

Another example:

```bash
python assignment_10_masked_arrays.py
```

### Step 8: Run Test Cases

```bash
python test_numpy_assignment.py
```

If `python` does not work, try:

```bash
py test_numpy_assignment.py
```

Expected test result:

```text
OK
```

## Files Included

Each assignment section has a separate Python script. The `run_all.py` file runs all sections together. The `test_numpy_assignment.py` file verifies the major operations using unit tests.

## GitHub Repository

Repository: `PradeepBhosale2005/ai-mastery-journey`

Assignment folder: `python-numpy-assignment-04`

## Assignment Status

Completed for NumPy Assignment 04 in NEURALIS Phase 2.1.
